[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.build]
cmds = ["npm run build"]

# Explicitly tell Nixpacks NOT to include these variables in the build environment
# This fixes the "SecretsUsedInArgOrEnv" error
[variables]
OPENAI_API_KEY = ""
SUPABASE_SERVICE_ROLE_KEY = ""
